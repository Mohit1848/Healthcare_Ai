apiVersion: v1
kind: Secret
metadata:
  name: openai-secret
  namespace: healthcare-ai
type: Opaque
stringData:
  OPENAI_API_KEY: "replace-me"
  # Optional: enables Groq Llama fallback if OpenAI is unavailable.
  GROQ_API_KEY: ""
